'use strict';

let assert = require('power-assert');
let slack = require('../slack.js');

describe('slack', () => {
  describe('#constructAttachments', () => {
    it('should return Slack attachment', () => {
      assert(1 === 2);
    });
  });
});
